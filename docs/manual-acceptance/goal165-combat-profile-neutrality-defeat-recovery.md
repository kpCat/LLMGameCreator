# Goal165 combat profile neutrality and defeat recovery

Goal165 is a GREEN acceptable automated candidate. It is not accepted by a human or Codex, creates no manual gate and requires an independent audit.

The Goal164 audit P1 is recorded as `BLOCKED_AT_15A8F2AB` and closed by profile-neutral route truth. A generated combat contract can be BasicAttack-only, package-ability-only or both; nonrequired unavailable routes are vacuously passed, and old Goal164 v4 both-route history remains `CAMPAIGN_CURRENT`.

After a genuine Runtime defeat, the player sees `Поражение` and can choose `Повторить встречу`, `Продолжить с сохранения` or `Начать новую игру`. Retry restores the exact in-memory checkpoint and performs one `StartEncounter` with zero Runtime Start calls. Save continuation performs zero Runtime Start calls; New Game performs one. Checkpoint truth drift produces `STALE_PROJECT` before any Runtime command.

Runtime, GamePackage, save schema, Unity, standalone and RC implementation remain unchanged. The physical core-only portable copy has no operational pointer and does not claim false RC readiness.

Goal166 independently recorded the remaining P1 identity/evidence gap as `BLOCKED_AT_0027404D` and closed it with an exact qualified-action catalog plus an end-to-end production campaign recovery route. Goal165 remains GREEN and `accepted=false`; it has no human gate.
