# Goal161S immutable standalone publication closure

Status: BLOCKED; accepted=false; no human gate.

The executed player tree is immutable at `%LOCALAPPDATA%/LGC/O/<project-token>/runs/r-<attempt>`. The one authorized smoke was GREEN at the player/output layer: preflight 13/13, legacy compatibility, exit 0, five markers, Player.log, run-status GREEN and atomically validated `current.json`; host reuse=true, host rebuild=false and Unity starts=0.

The returned controller result is `FAILED` at `release_candidate_record` with exact diagnostic `rc.payload.missing`. Retry=0. The immutable run and current pointer remain for forensics; RC CURRENT and portable all-selectable/core-only are not claimed.

Goal161T closure: this historical S run is the retained real qualification input. Its validated immutable pointer/run, five markers, exit 0, GREEN run-status, Player.log and cache-reuse facts remain unchanged. T now reads that authority, correlates payload/standalone/build history and writes RC `CURRENT` with zero Player, Unity or standalone invocations. The S record stays historical and unaccepted; no human gate is created.
