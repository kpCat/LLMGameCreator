# Goal158 generated-region travel Runtime and standalone vertical slice

Status: `GREEN_ACCEPTABLE_CANDIDATE`

Goal157 independent audit intake is `GREEN_ACCEPTABLE_CANDIDATE_AT_8939AEA0`. Goal158 keeps the unchanged strict v1 generated source, plan/rule/tiny/MVP/overlay/base chain and Lane A AcceptedMechanics/Social authority.

Lane B adds a deterministic travel overlay derived from exact strict-plan region-to-map bindings and directed connections. It appends one generic travel prototype and one safe gate per connection, then activates generated start and project identity. The real `IGameRuntime` route performs a generated origin interaction, deterministic navigation, at least one atomic `PlayerCommand.Interact` map transition with `MapChanged`, and a generated destination interaction. Primary hashes and RuntimeFrames belong to that complete route; replay and map-state roundtrip are exact.

History/UI restore `TRAVEL_CURRENT`; old Goal157 history restores only `START_CURRENT`. Exactly one cached hidden standalone smoke must reuse the host, rebuild nothing and start Unity zero times. The actual payload, RC CURRENT and a portable copy retain travel and accepted-mechanics truth without execution.

No user action or manual Unity review is requested. `goal158Accepted=false`, `goal158ManualReviewRequired=false`, and the next action is an independent Goal158 audit followed by planning transactional seed regeneration.
