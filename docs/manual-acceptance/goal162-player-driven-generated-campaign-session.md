# Goal162 player-driven generated campaign session

Status: implementation `GREEN_ACCEPTABLE_CANDIDATE`; independent audit `BLOCKED_AT_8164185B`; `accepted=false`; no human gate.

Goal162C closes the incomplete manually published scaffold at `92d419862443e84e2bfafa02f1a985edd96a5889`. The WinForms generated-campaign page is a player-facing development workspace over existing qualified Runtime commands and generated-save services.

Goal163 independent audit intake reproduced two P1 product-truth defects: campaign combat used an Application-time cloned package with a transient fixed-power attack, and generated quest completion was automatic with `CompleteQuest` count 0. Goal163 removes that mutation/rewrite and implements read-only readiness plus one manual CompleteQuest contract. The real package still cannot execute its generated current-region quest encounters, so generated victory/manual turn-in evidence remains blocked rather than synthetic.

All-selectable and core-only generated projects pass the complete route. Core-only never claims false RC readiness. Legacy/template projects remain unavailable to the campaign page and retain their existing raw snapshot workflow. Runtime Simulator is unchanged. Primary UI exposes human titles and descriptions, with technical IDs/hashes/paths confined to collapsed diagnostics.

Protected release-candidate record, immutable standalone run, current pointer, standalone history, Goal142 source and `goal148-manual` source bytes remain identical. Player starts=0, Unity starts=0 and standalone Build calls=0. No manual test is required or permitted for this goal.

Next action: `repair_generated_encounter_runtime_contract_then_repeat_goal163_real_matrix`.
