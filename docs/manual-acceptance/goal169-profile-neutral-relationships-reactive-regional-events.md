# Goal169 profile-neutral relationships and reactive regional events

Status: `BLOCKED_AFTER_SINGLE_HIDDEN_SMOKE_MOVE_FRAME_ASSERTION`.

Goal168 implementation remains GREEN, but its independent audit is `BLOCKED_AT_BBFD46A2`. Goal169 closes both P1 findings: relationship qualification and history execute only branches actually present and persist per-relationship `Available`/`Required`/`Passed` truth, while exact combat accepts exact health, stat and status effects only when Runtime changes encounter state. Build save-continuation truth is honestly `NOT_EVALUATED_AT_BUILD`/`false`; the separate real save matrix is GREEN.

Generated relationship branches derive Support gratitude, Challenge aftermath and Refusal fallout events. Event identity and resolution flag equal the exact generated event dialogue ID. Placement is deterministic, safe and reachable for arbitrary map sizes and event counts. The overlay adds only event prototypes, dialogues, interactions, map entities and event metadata; prior package records, relationships, combat, travel, manifest/generated content and source sidecars remain exact.

Ordinary relationship prerequisite, Move, Interact, OpenDialogue and ChooseDialogueOption routes prove `LOCKED`, `AVAILABLE` and exactly-once `RESOLVED`. Support reputation derives from the final quest reward; Challenge and Refuse do not duplicate their original consequence. The state-backed «События мира» tab, human map markers and consequence projection expose no raw IDs, hashes or paths.

History v7 is `REGIONAL_EVENTS_CURRENT`; genuine v6 remains relationships-current/events-pending/`PROJECT_NOT_READY` with «Собрать и играть». Regeneration and rollback seal the relationship branch matrix and event summary/overlay/inventory. Exact AVAILABLE and RESOLVED continuation, package rebase, compatible preservation, incompatible drop and no-ghost migration pass.

Of 108 discovered behavioral cases, all 107 non-smoke cases pass and the sole hidden-smoke case is blocked. Exactly one cached hidden smoke reused the host, rebuilt nothing, started Unity zero times, exited 0 with 5/5 self-checks and received no retry. RC and portable all-selectable are current; portable core-only does not claim false RC readiness. Goal142, Goal148, generation sidecars and the cached host remain byte-identical. Goal169 payload qualification nevertheless failed because the 84 movement frames used direction-only titles and no frame had the required explicit `Move.` prefix. The post-smoke fix emits `Move.<Direction>` while retaining `PlayerCommand.Move`; it is covered by non-smoke contracts, but the consumed smoke was not rerun and retry remains zero.

Goal169 is not accepted by a human or Codex. It creates no manual gate and requires an independent blocker audit/follow-up without reinterpreting the consumed smoke as GREEN.
