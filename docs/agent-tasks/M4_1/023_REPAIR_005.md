# 023_REPAIR_005.md — repair M4_1_005 markdown recommendation proof coverage

This task is repair-only guidance for the current M4.1 execution branch. If implementation conflicts with this file, stop and report.

## Header

Task ID: `M4_1_005_REPAIR`

Milestone: `M4.1 real-model evaluation gate`

Status: `ready_with_user_approval`

Depends on:

```text
- M4_1_005 was attempted.
- Review found incomplete proof coverage.
- check-all baseline is available.
```

Unlocks:

```text
- M4_1_006 only after this repair passes.
```

Risk level: low

Expected changed files count: 1-3

## Gate status

Allowed before current gate review: yes

Requires user approval: already approved by `.devflow/NEXT_TASK.md`.

## Source of truth

Source-of-truth docs:

```text
docs/agent-tasks/M4_1/M4_1_005_EVALUATION_MARKDOWN_GOLDEN_RECOMMENDATIONS.md
docs/GENERATOR_PLAN_STRICT_LLM_EVALUATION.md
docs/CURRENT_GENERATOR_STATE.md
docs/agent-tasks/_TEST_QUALITY_RULES.md
docs/agent-tasks/_FIXTURE_AND_GOLDEN_RULES.md
docs/agent-tasks/_DIFF_HYGIENE_RULES.md
```

Existing patterns to inspect:

```text
src/LLMGameCreator.Application/Design/GeneratorPlans/GeneratorPlanStrictLlmEvaluationMarkdownRenderer.cs
tests/LLMGameCreator.Tests/Design/GeneratorPlanStrictLlmEvaluationMarkdownRendererTests.cs
```

## File boundaries

Allowed files:

```text
tests/LLMGameCreator.Tests/Design/GeneratorPlanStrictLlmEvaluationMarkdownRendererTests.cs
tests/fixtures/strict-llm-evaluation-markdown/**
src/LLMGameCreator.Application/Design/GeneratorPlans/GeneratorPlanStrictLlmEvaluationMarkdownRenderer.cs
.devflow/CURRENT_RUN.md
.devflow/NEXT_TASK.md
```

Production renderer changes are allowed only if required to make the required proof tests pass. Prefer adding missing proof tests first.

Forbidden files:

```text
src/LLMGameCreator.Runtime/**
src/LLMGameCreator.Scripting/**
src/LLMGameCreator.GamePackage/**
src/LLMGameCreator.WinForms/**
LLMGameCreator.sln
*.csproj
docs/CURRENT_GENERATOR_STATE.md
docs/CURRENT_GENERATOR_STATE.json
```

Deleted files: none.

## Exact repair goal

Repair the M4_1_005 result by adding the missing proof coverage from the original task spec.

The current repository must prove all of the following behaviors:

```text
- high JsonInvalidCount recommends strict JSON prompt/parser/repair hardening;
- pass rate 1.0 and zero failures can say the contract looks stable;
- stable/high-pass markdown must not imply automatic M5/M6 unlock;
- warnings remain visible when pass rate is high;
- rendering the same result twice is deterministic;
- empty diagnostics and samples do not crash and render explicit no-data messages;
- required report sections are present.
```

## Required proof tests

Add or strengthen tests so they assert exact strings/absence conditions, not vague success.

Required assertions:

```text
- markdown contains "## Summary"
- markdown contains "## Per-contract summary"
- markdown contains "## Diagnostic hot spots"
- markdown contains "## Samples"
- markdown contains "## Recommendations"
- high JSON failure scenario contains the exact recommendation text currently used by the renderer for strict JSON prompt/output hardening
- high-pass stable scenario contains "Contract looks stable"
- high-pass stable scenario does not contain "M5"
- high-pass stable scenario does not contain "M6"
- warning scenario contains "## Quality warnings"
- warning scenario contains the warning diagnostic code
- warning scenario contains the warning message
- empty diagnostics scenario contains "_No diagnostics were reported._"
- empty warnings scenario contains "_No quality warnings were reported._"
- empty samples scenario contains "_No samples were captured._"
- Render(theSameResult) equals Render(theSameResult)
```

Use exact current renderer wording unless a tiny renderer wording change is necessary. Do not introduce broad markdown snapshots.

## Non-goals

```text
- Do not change evaluation execution.
- Do not call provider/LLM.
- Do not redesign report format beyond tiny wording needed for tests.
- Do not update CURRENT_GENERATOR_STATE.
- Do not start M4_1_006.
```

## System gates

Focused test command:

```powershell
dotnet test tests\LLMGameCreator.Tests\LLMGameCreator.Tests.csproj --configuration Debug --filter "FullyQualifiedName~GeneratorPlanStrictLlmEvaluationMarkdownRendererTests"
```

Full gate:

```powershell
powershell -ExecutionPolicy Bypass -File .\.devflow\scripts\check-all.ps1
```

## Stop conditions

Stop if:

```text
- required source files are missing and no exact equivalent exists;
- satisfying tests would require UI/runtime/GamePackage/schema/project changes;
- renderer wording would imply automatic M5/M6 unlock;
- task requires more than 3 changed files;
- check-all fails after 2 repair attempts.
```

## Expected final report

Final report must include:

```text
- Task ID M4_1_005_REPAIR;
- files read;
- files changed;
- exact proof tests added/strengthened;
- focused test result;
- check-all result;
- confirmation no M5/M6 unlock wording was introduced;
- confirmation M4_1_006 was not started.
```

## Next task pointer

On success, recommend but do not execute:

```text
# NEXT_TASK

Mode: single-task
Task source: agent_task_spec
Task id: M4_1_006
Task spec file: docs/agent-tasks/M4_1/M4_1_006_STRICT_REPAIR_PROMPT_GUARDRAILS.md
Reason: Add guardrails for repair prompts after parser diagnostics and evaluation recommendations are covered.
User approval: required
Expected stop after completion: yes
```
