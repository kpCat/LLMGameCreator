# Goal 081 — Edit-Driven GamePackage Runtime Preview Playthrough

This task pack is intentionally repo-relative. It should be unpacked into the repository root, then launched through `CODEX_LAUNCHER.txt`.

Goal 081 continues the edit-driven spine after Goal 080. Goal 080 produced a disk-backed existing-schema projected GamePackage and runtime-preview bridge proof. Goal 081 must consume that projected GamePackage as actual session input and produce a deterministic player-facing playthrough transcript/hash chain using existing Application/GamePackage/runtime-preview seams. It must not mutate public schema, Runtime, Unity, provider/LLM/RAG/media, Lua/Scripting, generator-library, or project files.
