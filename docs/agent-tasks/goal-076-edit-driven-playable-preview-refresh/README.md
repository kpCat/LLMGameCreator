# Goal 076 task package

Этот архив надо распаковать **в корень репозитория** `C:\Users\endim\LLMGameCreator\`.

После распаковки главный файл задачи будет здесь:

```text
docs/agent-tasks/goal-076-edit-driven-playable-preview-refresh/GOAL.md
```

Короткий launcher для Codex можно отправлять отдельным сообщением; он должен ссылаться на этот repo-relative файл.

# Goal 076 — Edit-Driven Playable Preview Refresh & Unity Handoff Smoke

Этот архив — готовая задача для Codex.

Главный файл задачи:

```text
GOAL.md
```

Рекомендуемый launcher-текст для Codex находится в:

```text
CODEX_LAUNCHER.txt
```

Задача рассчитана как aggressive composite goal после Goal 075A. Она должна встроить user handoff для Goal 075, не делать отдельную acceptance-only задачу, и перевести edit/validate/apply loop в следующий реальный шаг: deterministic playable preview refresh + GamePackage/Unity handoff proof.
