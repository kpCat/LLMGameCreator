# Goal 077 — Edit-Driven Review Package Materialization

This task pack is repo-relative and must be unpacked into the repository root before running Codex.

Goal 077 turns Goal 076 sidecar refresh proof into a deterministic, disk-backed review package that can be read and validated by smoke tests and WinForms workspace UI without changing public GamePackage schema, Runtime, Unity, Lua, provider, generator-library, .sln or .csproj files.
