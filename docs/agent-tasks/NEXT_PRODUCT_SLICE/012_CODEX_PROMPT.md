Работай в репозитории `LLMGameCreator`.

Выполни задачу:

```text
docs/agent-tasks/NEXT_PRODUCT_SLICE/012_GENERATOR_CATALOG_CONTRACT_FOUNDATION.md
```

Это bounded foundation slice:
- добавить GeneratorModuleManifest;
- добавить GeneratorCatalog / BuiltInGeneratorCatalog;
- добавить GeneratorCatalogValidator;
- добавить простой GeneratorPlanResolver;
- добавить current/planned generator manifests;
- добавить smoke `generator-catalog-contract`.

Не делай UI.
Не трогай Runtime.
Не меняй GamePackageDefinition/package schema.
Не трогай WinForms.
Не трогай generator-library.
Не делай dynamic plugins.
Не исполняй generators.
Не делай semantic world model.
Не делай imported map/lazy world/procedural quest engine.
Не вызывай LLM/provider.
Не запускай git-команды.

Рекомендуемый reasoning level: High.

Обязательные проверки указаны в task file.

Финальный отчёт дай на русском.
