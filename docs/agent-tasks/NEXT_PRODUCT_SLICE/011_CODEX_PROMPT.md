Работай в репозитории `LLMGameCreator`.

Выполни задачу:

```text
docs/agent-tasks/NEXT_PRODUCT_SLICE/011_GAME_BLUEPRINT_CAPABILITY_GRAPH.md
```

Это bounded foundation slice:
- добавить `GameBlueprint`;
- добавить `CapabilityDefinition` / registry / compatibility validator;
- добавить начальные built-in capabilities;
- добавить blueprint presets;
- добавить smoke `game-blueprint-capability-compatibility`.

Не делай UI wizard.
Не трогай Runtime.
Не меняй `GamePackageDefinition.cs` или package schema.
Не трогай WinForms.
Не трогай generator-library.
Не делай semantic world model.
Не делай generator catalog plugins.
Не делай imported map/lazy world/procedural quest engine.
Не вызывай LLM/provider.
Не запускай git-команды.

Рекомендуемый reasoning level: High.

Обязательные проверки указаны в task file.

Финальный отчёт дай на русском.
