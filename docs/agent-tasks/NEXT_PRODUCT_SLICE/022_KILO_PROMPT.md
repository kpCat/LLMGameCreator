Работай в текущем рабочем дереве репозитория LLMGameCreator.

Выполни задачу:

```text
docs/agent-tasks/NEXT_PRODUCT_SLICE/022_PROVIDER_OUTPUT_INTAKE.md
```

Не создавай ветки.
Не переключай ветки.
Не делай merge/rebase/cherry-pick.
Не запускай git-команды.
Branch management выполняет пользователь вручную.

Это bounded backend-срез:
- fulfillment state scanner;
- missing/available/invalid status;
- fulfilled asset/audio/lua indexes;
- invalid outputs report;
- materialization integration;
- validation;
- product smoke `unity-archive-fulfillment-state`.

Не реализуй Unity.
Не создавай Unity project.
Не трогай Runtime.
Не меняй GamePackageDefinition/package schema.
Не трогай WinForms.
Не трогай generator-library.
Не вызывай LLM/provider.
Не исполняй generators.
Не исполняй Lua.
Не реализуй ComfyUI/Suno integration.
Не создавай реальные asset/audio/Lua expected output files в materialization.

Рекомендуемый executor: Kilo Code first.
Рекомендуемый reasoning level: High.

Финальный отчёт дай на русском.
