# LLMGameCreator Product Slice 011 Pack

## Slice

`Product Slice 011: GameBlueprint + Capability Graph Compatibility Foundation`

## Apply

Unzip this archive at repository root.

Then give Codex:

```text
docs/agent-tasks/NEXT_PRODUCT_SLICE/011_CODEX_PROMPT.md
```

## Recommended reasoning

High.

## Verification policy

This slice is backend/headless. The user should not need to click through the app for acceptance unless Codex changes visible UI layout. Repository diff, unit tests, product smoke, state/docs consistency and `check-all` are the acceptance path.
