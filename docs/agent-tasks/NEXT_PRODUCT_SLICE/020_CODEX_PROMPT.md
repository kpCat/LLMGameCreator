Работай в репозитории `LLMGameCreator`.

Выполни задачу:

```text
docs/agent-tasks/NEXT_PRODUCT_SLICE/020_REQUEST_PIPELINE.md
```

Это крупный, но bounded backend-срез:
- asset request queue;
- audio request queue;
- Lua/data module request queue;
- materialization integration;
- validation;
- smoke `unity-archive-request-pipeline`.

Не реализуй Unity.
Не создавай Unity project.
Не трогай Runtime.
Не меняй GamePackageDefinition/package schema.
Не трогай WinForms.
Не трогай generator-library.
Не вызывай LLM/provider.
Не исполняй generators.
Не исполняй Lua.
Не реализуй ComfyUI/Suno integration.
Не запускай git-команды.

Не делай broad repository discovery. Читай только read-first список и ближайшие явно нужные файлы.

Рекомендуемый reasoning level: High.

Финальный отчёт дай на русском.
