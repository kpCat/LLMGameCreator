Работай в репозитории `LLMGameCreator`.

Выполни задачу:

```text
docs/agent-tasks/NEXT_PRODUCT_SLICE/013_CATALOG_BACKED_COMPOSITION_DIAGNOSTICS.md
```

Это bounded foundation slice:
- добавить consolidated composition diagnostics/report;
- объединить GameBlueprint capability validation и GeneratorCatalog planning;
- добавить markdown renderer;
- добавить deterministic recommended actions;
- добавить smoke `composition-diagnostics-report`.

Не делай UI.
Не трогай Runtime.
Не меняй GamePackageDefinition/package schema.
Не трогай WinForms.
Не трогай generator-library.
Не исполняй генераторы.
Не делай dynamic plugins.
Не делай semantic world model.
Не делай imported map/lazy world/procedural quest engine.
Не вызывай LLM/provider.
Не запускай git-команды.

Рекомендуемый reasoning level: High.

Обязательные проверки указаны в task file.

Финальный отчёт дай на русском.
