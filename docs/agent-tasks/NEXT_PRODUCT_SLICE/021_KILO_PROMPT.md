Работай в новой ветке от `main`, например:

```text
kilo-s021-provider-plan
```

В репозитории `LLMGameCreator` выполни задачу:

```text
docs/agent-tasks/NEXT_PRODUCT_SLICE/021_PROVIDER_JOB_PLAN.md
```

Это крупный bounded backend-срез для Kilo Code:
- fulfillment slots;
- provider job plan;
- provider-specific job JSON files;
- materialization integration;
- validation;
- product smoke `unity-archive-provider-job-plan`.

Не реализуй Unity.
Не создавай Unity project.
Не трогай Runtime.
Не меняй GamePackageDefinition/package schema.
Не трогай WinForms.
Не трогай generator-library.
Не вызывай LLM/provider.
Не исполняй generators.
Не исполняй Lua.
Не реализуй ComfyUI/Suno integration.
Не запускай git-команды.
Не делай broad repository discovery.

Рекомендуемый reasoning level: High.

Финальный отчёт дай на русском.
