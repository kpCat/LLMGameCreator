/goal

Выполни задачу из файла, который уже распакован в репозиторий:

docs/agent-tasks/goal-153b-declarative-parameter-constraints-domain-integrity-and-goal-quality-gate-hotfix/GOAL.md

Рекомендуемая модель Codex: GPT-5.6 Terra
Уровень мышления: High

Repo: https://github.com/kpCat/LLMGameCreator
Working copy: C:\Users\endim\LLMGameCreator\
Branch: main
Required base: 4325fe4745c6a1e2363e4d49403d2360404ab69e

Это новый отдельный диалог Codex. Весь обязательный контекст находится в GOAL.md.

План уже одобрен; не спрашивай подтверждение и не проси пользователя выполнять промежуточные проверки.

Критически важно:
- Goal153/153A пока НЕ приняты и manual gate не готов;
- удали module/parameter hardcoding из FeatureModuleParameterBindingService;
- relation abilityManaCost <= startingMana должна находиться в module JSON и исполняться generic evaluator;
- constraints должны входить в fingerprint/certification;
- startingMana до 1000 не может создавать Amount > Capacity;
- resource amount вне min/max отклоняется до Runtime;
- qualification-domain proof читает реальные parameter maxima, а не дублирует 1000 в тесте;
- добавь постоянную GOAL_DESIGN_QUALITY_POLICY и ссылку из AGENTS.md;
- static architecture scan запрещает Goal153 IDs в generic production C#;
- Unity Editor invocation budget = 0;
- используй существующий host cache и один hidden smoke;
- не запускай full suite, 85-case closure или all-ProductSmoke;
- создай и запушь ровно один финальный GREEN/BLOCKED/FAILED commit.

В финале сообщи declarative constraint, mana domain, architecture scan, fingerprint invalidation,
Goal153A regressions, cache reuse, Unity process count, final SHA, push, HEAD == origin/main,
clean worktree и короткий combined manual gate.
