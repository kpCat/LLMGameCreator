# Goal 078 — Edit-Driven Review Package Playable Session

This task pack is repo-root relative. It must be unpacked into `C:\Users\endim\LLMGameCreator\` before launching Codex with the launcher text.

Goal 078 consumes the real disk-backed Goal 077 review package and produces a deterministic, headless, player-like review session proof. It must move beyond package materialization into a package-loaded, action-replayed, state-hashed playable/simulatable loop while staying out of Runtime, Unity, GamePackage public schema, providers, Lua, generator-library, and project files.
