# Apply NEXT_TASK 006

This archive switches `.devflow/NEXT_TASK.md` to `M4_1_006`.

Apply from repository root:

```powershell
cd C:\Users\endim\LLMGameCreator
# unpack this archive into the repository root with overwrite
powershell -ExecutionPolicy Bypass -File .\.devflow\scripts\check-devflow-state.ps1
powershell -ExecutionPolicy Bypass -File .\.devflow\scripts\check-all.ps1
```

Then start a new Kilo session with the standard single-task prompt.
